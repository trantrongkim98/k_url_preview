name: kurlpreview
description: A new Flutter plugin.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  html: ^0.15.0
  cached_network_image: ^3.0.0
  flutter_cache_manager: ^3.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
